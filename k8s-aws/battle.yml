apiVersion: v1
kind: ConfigMap
metadata:
  name: battle-config
  namespace: elemental-duel
data:
  PORT: "8080"
  MYSQL_HOST: "battle-mysql"
  MYSQL_PORT: "3306"
  MYSQL_DATABASE: "elemental_duel"
  MYSQL_USER: "elemental"
  DB_RETRY_COUNT: "10"
  BACKUP_TARGET: "render-placeholder-storage"
---
apiVersion: v1
kind: Secret
metadata:
  name: battle-secret
  namespace: elemental-duel
type: Opaque
stringData:
  MYSQL_PASSWORD: elementalpass
  MYSQL_ROOT_PASSWORD: rootpass123
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: battle-mysql-pvc
  namespace: elemental-duel
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: battle-mysql
  namespace: elemental-duel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: battle-mysql
  template:
    metadata:
      labels:
        app: battle-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.4
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: battle-config
                  key: MYSQL_DATABASE
            - name: MYSQL_USER
              valueFrom:
                configMapKeyRef:
                  name: battle-config
                  key: MYSQL_USER
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: battle-secret
                  key: MYSQL_PASSWORD
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: battle-secret
                  key: MYSQL_ROOT_PASSWORD
          volumeMounts:
            - name: mysql-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-storage
          persistentVolumeClaim:
            claimName: battle-mysql-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: battle-mysql
  namespace: elemental-duel
spec:
  selector:
    app: battle-mysql
  ports:
    - port: 3306
      targetPort: 3306
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: battle-api
  namespace: elemental-duel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: battle-api
  template:
    metadata:
      labels:
        app: battle-api
    spec:
      containers:
        - name: battle-api
          image: protodoc/elemental-duel:battle-latest
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: battle-config
                  key: PORT
            - name: MYSQL_HOST
              valueFrom:
                configMapKeyRef:
                  name: battle-config
                  key: MYSQL_HOST
            - name: MYSQL_PORT
              valueFrom:
                configMapKeyRef:
                  name: battle-config
                  key: MYSQL_PORT
            - name: MYSQL_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: battle-config
                  key: MYSQL_DATABASE
            - name: MYSQL_USER
              valueFrom:
                configMapKeyRef:
                  name: battle-config
                  key: MYSQL_USER
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: battle-secret
                  key: MYSQL_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: battle-api
  namespace: elemental-duel
spec:
  selector:
    app: battle-api
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: battle-mysql-backup
  namespace: elemental-duel
spec:
  schedule: "0 */6 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          # Step 1: dump MySQL to a shared emptyDir volume
          initContainers:
            - name: dump
              image: mysql:8.4
              command: ["/bin/sh", "-c"]
              args:
                - >
                  mysqldump -h battle-mysql
                  -u$MYSQL_USER -p$MYSQL_PASSWORD
                  $MYSQL_DATABASE > /backup/dump.sql
              env:
                - name: MYSQL_DATABASE
                  valueFrom:
                    configMapKeyRef:
                      name: battle-config
                      key: MYSQL_DATABASE
                - name: MYSQL_USER
                  valueFrom:
                    configMapKeyRef:
                      name: battle-config
                      key: MYSQL_USER
                - name: MYSQL_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: battle-secret
                      key: MYSQL_PASSWORD
              volumeMounts:
                - name: backup-volume
                  mountPath: /backup
          # Step 2: upload the dump to S3
          containers:
            - name: upload
              image: amazon/aws-cli:latest
              command: ["/bin/sh", "-c"]
              args:
                - >
                  aws s3 cp /backup/dump.sql
                  s3://$S3_BUCKET/battle-backups/battle-$(date +%F-%H%M%S).sql
              env:
                - name: AWS_ACCESS_KEY_ID
                  valueFrom:
                    secretKeyRef:
                      name: aws-credentials
                      key: AWS_ACCESS_KEY_ID
                - name: AWS_SECRET_ACCESS_KEY
                  valueFrom:
                    secretKeyRef:
                      name: aws-credentials
                      key: AWS_SECRET_ACCESS_KEY
                - name: AWS_DEFAULT_REGION
                  valueFrom:
                    secretKeyRef:
                      name: aws-credentials
                      key: AWS_DEFAULT_REGION
                - name: S3_BUCKET
                  valueFrom:
                    secretKeyRef:
                      name: aws-credentials
                      key: S3_BUCKET
              volumeMounts:
                - name: backup-volume
                  mountPath: /backup
          volumes:
            - name: backup-volume
              emptyDir: {}
